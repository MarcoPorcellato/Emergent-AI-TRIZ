Complete the separately preregistered A0-R2-C2 SmolLM2 shape correction in
docs/A0R2C2_SHAPE_CORRECTION.md from the live public main anchor. Preserve C1
as an immutable terminal failure; C2 is not a retry under the consumed C1
authorization.

Change only the validated singleton Llama batch-axis normalization in new
C2-namespaced code. Keep exact model/revision, corpus, sealed targets, prompts,
sites, statistics, thresholds, resource caps, no-network/no-generation policy,
no-tuning rule, and terminal-publication policy unchanged.

Merge and exact-head qualify the C2 contract before requesting a new explicit
one-attempt authorization. Do not load a model or read targets until that
authorization is recorded and CCP reports resource Admit, inactive admission,
and an empty queue. Publish every terminal outcome without claim promotion.
