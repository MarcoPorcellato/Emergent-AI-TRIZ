Publish and verify the terminal A0-R2-C2 SmolLM2 failure package from the live
public main anchor. Preserve C1 as an immutable terminal failure; C2 consumed
its separately authorized one-attempt execution contract and is not retryable.

The C2 singleton-batch correction successfully extracted the frozen
representations, then terminated with an `A0R2AnalysisError` at the data stage.
Keep the exact model/revision, corpus, targets, prompts, sites, statistics,
thresholds, resource caps, no-network/no-generation policy, no-tuning rule,
and terminal-publication policy unchanged.

Publish the authorization receipt, activation receipt, representation index,
external dense-asset locator and hash, failure receipt, report, and manifest.
Do not rerun, tune, substitute, or reopen targets under the consumed C2
authorization. Keep all claim IDs absent and record the precise recovery gate
for any separately preregistered future study.
