Complete A0-R2-C3 according to docs/A0R2C3_ANALYSIS_ONLY_RECOVERY.md, starting
from verified public main `06d26d82604e1c644950f0343196a8991c6a6255` and
verifying the live repository, ruleset, artifacts, and receipts first.

C3 is an analysis-only recovery of the immutable C2 activation bundle. Keep
the exact model/revision, frozen protocol, corpus, targets, prompts, endpoint,
statistics, thresholds, claims, and source artifact bytes unchanged. Do not
load or query a model, generate output, tune, substitute inputs, or reopen C2
targets under its consumed authorization.

First finish and publish C3.0/C3.1 with synthetic qualification, the canonical
SmolLM2 runtime contract, C3-namespaced pre-analysis recovery and publication,
and exact recovery bindings. Preserve byte-for-byte every R2 module bound by
the consumed R2 dossier; never amend its implementation manifest. Then request
a new explicit authorization for one C3
analysis-boundary target read; the explicit authorization receipt is now recorded
against the immutable contract; after CCP resource `Admit` with an
inactive empty queue, run the one analysis-only recovery and publish every
terminal outcome. Keep all claim IDs absent and complete only after fresh-clone
verification and terminal exact-head gates. The single C3 analysis-only run has
now produced a positive exploratory package; fresh-clone verification and its
publication PR remain the final gates.
