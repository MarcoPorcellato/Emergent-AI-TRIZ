EXP-002 checkpoint (2026-08-20): branch `exp002-qwen3-followup` is the active
implementation branch at implementation checkpoint `e47937c` (followed by
documentation-only checkpoint commits). The no-model protocol,
seven-model identity registry, target-free 351-question TRIZ bank,
tokenizer-audit gate, public label-surface diagnostic, injected material
runner, immutable publication contracts, guarded EXP-002A CLI, direct-answer
review gate, direct-question and label-free candidate paths, locator-only
source-familiarity boundary, deterministic EXP-002C power calibration,
target-free corpus validator, and the `not_ready` sealed transfer target-key
contract are frozen;
the seven-model EXP-002A baseline is published with terminal `null` packages.
The exact seven-model material dossier is operator-authorized at approval hash
`0c5943ad5a7bf2c598511b8c3ecc29bd566f33140af59c8c6d788f2423483d67`.
The CCP `origin/main` runbook
`5f2ef665be4dc47fd354befcba53251a4e51744f` is the current coordination
contract; the prior baseline used an isolated exact
binary; all seven runs had Admit/inactive/queue-0 preflights and clean
post-run handoffs. The installed older binary remains stale and must not be
used for future material work. The next objective is to freeze and execute the
remaining response-surface, direct TRIZ/source-familiarity, and new blinded
transfer stages; do not rerun the seven baseline packages.

Complete the canonical improvement plan in `docs/LABORATORY_MASTER_PLAN.md`,
using `docs/HYPOTHESES_AND_FALSIFICATION.md` as the normative Weak/Strong
contract. The current public checkpoint is `main` at
`0123ce467408becbf127b66da1fcd4166bbbd431`; verify `origin/main`, the active
ruleset, open pull requests, and receipt branches live before the next
publication or mutation.

Treat A0/R1 positives, the R3 reference-integrated package, and the seven-model
comparative null packages as immutable exploratory
evidence. Do not rerun or tune them. The immediate priority is the real-human
H1 gate: three independent TRIZ experts and immutable raw/agreement,
abstention, disagreement, adjudication, and keep/amend receipts. Then freeze
stronger lexical, template, source, near-neighbour, generic, cosmetic,
Matrix-direction, unsupported-edge, abstention, random-label, and extreme
domain-shift controls; demonstrate an out-of-sample direction; and prepare one
validated-principle Lab 06 causal dossier. Defer composition and broad model
scaling until that causal gate. A separate no-download dossier now proposes
GPT-2 and SmolLM2-135M are now completed one-run architecture/scale controls;
their terminal outcomes are null, non-pooled, and not a general TRIZ claim.
The official-source runtime/tokenizer contract and the pre-access failure
prevention checklist are maintained in docs/EXP001_ADDITIONAL_MODEL_RUNTIME.md;
SmolLM2-135M is Llama architecture but declares GPT2Tokenizer.
Keep Track B as a separate no-TRIZ-term
from-scratch protocol with independent seeds and checkpoints. Its no-model
contract is now recorded under `experiments/track-b-emergence/`; training and
corpus access remain forbidden until a separate exact dossier and approval.

Do not load models, read sealed targets, train models, or run CCP until the
relevant no-model contract, hashes, resource envelope, and explicit operator
approval are complete. Publish every positive, null, failed, or
non-interpretable result; preserve historical artifacts; use deterministic
tools first; delegate bounded inventory, tests, and documentation to cheaper
workers; require a no-model contributor quickstart; and keep architecture,
statistics, security, CCP, publication, and merge decisions under primary
review. The current public closeout is `main`
`0123ce467408becbf127b66da1fcd4166bbbd431`; all comparative execution and
publication gates are complete. Continue only with the human/external H1 gate
or another separately frozen and authorized protocol.

The additional model execution packages are published under
`results/exp001-comparative/` with public locators and hashes for dense assets
retained locally; a fresh clone must reject those missing assets fail-closed.
The earlier R3 SmolLM2 package remains published in main merge
`4cc1c6d862bffc9558b47a5cadd838a2ee22c465` with terminal `null`, one sealed
target read, and no promoted scientific claim; the current objective extends
the comparative proposal without reopening that package.
The exact acquisition/one-run approval for
EleutherAI/gpt-neo-125m@21def0189f5705e2521767faed922f1f15e7d7db and
Qwen/Qwen2.5-0.5B@060db6499f32faf8b98477b0a26969ef7d8b9987 is now fully
consumed and published. It
bound 529,444,041 bytes across eight GPT-Neo files and 999,586,188 bytes across
seven Qwen2.5 files, with 1 GiB and 1.5 GiB ceilings. Preserve all seven
packages and their non-pooled terminal outcomes; do not rerun them.

The official documentation audit is recorded in
`docs/EXP001_MODEL_OFFICIAL_DOC_AUDIT.md`; it is metadata-only and does not
authorize model access. Before any material run, enforce the exact checkpoint
config, the Qwen 32,768 effective context bound, GPT-Neo right-padding rule,
fast-tokenizer boundary, offline/no-generation mode, and receipt fields from
that audit.

Acquisition checkpoint (2026-08-19): the exact authorized GPT-Neo and Qwen2.5
snapshots are integrity-verified. Receipts are
`results/exp001-comparative/preexecution/gpt-neo-125m-integrity-receipt.json`
and `results/exp001-comparative/preexecution/qwen2.5-0.5b-integrity-receipt.json`;
their model SHA-256 values are `52738cbfb54e25a232598242f60ef19ee193d36090b98fe649b10c02724b3521`
and `88c142557820ccad55bb59756bfcfcf891de9cc6202816bd346445188a0ed342`.
No model load, generation, dense output, or sealed-target access had occurred
at the acquisition checkpoint. That checkpoint is now complete; no further
model or target access is authorized by this dossier.

Execution checkpoint (2026-08-19): both one-shot runs completed and are
terminal `null`. GPT-Neo is published in
`results/exp001-comparative/gpt-neo-125m-21def018-gpt-neo-125m-20260819-01`
with p=.6875, 323.899 s, peak RSS 1,858,076,672 B, and dense asset hash
`53222272e786045156d86ad8aaf9ea123bc8e6efa26778d39008809393850c52`.
Qwen2.5 is published in
`results/exp001-comparative/qwen2.5-0.5b-060db649-qwen2.5-0.5b-20260819-01`
with p=.96875, 935.267 s, peak RSS 4,876,828,672 B, and dense asset hash
`577bea7e3161de630e5e9a649c152d301bc01658827a90d26d24eda48c627b3c`.
Each package records one sealed-target read, no network/generation, and empty
claim IDs. Qwen2.5 publication is merged in `main` at
`a081e612feca348b28253ee0cc9e67c24b45ed3a` (PR #97); GPT-Neo publication is
merged at `74926e15fe06a6e41ef1aa0d96e731b70636531a` (PR #96). Fresh-clone
verification passed for both packages with the declared external assets and
rejected both missing and one-byte-mutated assets. No further runs of these
models are authorized by this dossier.

The next scientific gate is human construct validation: three independent TRIZ
experts, blinded ratings, agreement/abstention/disagreement receipts, and a
keep/amend decision. Until that gate is closed, do not scale models, pool
results, or promote any TRIZ claim.
