Complete `docs/EXP001_REFERENCE_INTEGRATED.md` from verified public main
`db4cf6d32d263f1df059f6fd376d4cb2bfd38a9c`, resuming local R3 checkpoint
`fe77dc6560e95dbb865cc1bf721552fd610782b5` only after verifying the live repository,
ruleset, source corpus, artifacts, and receipts before acting.

R3.0 target-free freeze is being completed on branch `agent/exp001-r3-reference-freeze`: a strict
no-model protocol, sparse externally-referenced Matrix 2003 fixture, and
rights-aware Panitz edge fixture are being prepared. Do not call this a frozen
study until paired items, split/contamination receipts, implementation hash
bindings, synthetic qualification, and an exact approval dossier exist.
The public target-free fixture structurally satisfies the six-domain primary
design. The protocol is frozen for no-model preparation; finish and verify the
implementation, freeze manifest, approval dossier, and exact-head
qualification before any model or sealed-target access. Resume instructions
are in `docs/EXP001_R3_RESTART_HANDOFF.md`.

Prepare and freeze a new R3/EXP-001 TRIZ reference-integrated study without
changing any A0-R2/C3 bytes. Keep `TRIZ-blinded-transfer` and
`source-exposed-competence` physically and statistically separate; include the
40 principles, a double-checked sparse Matrix 2003 fixture, and a rights-aware
Panitz tool-edge fixture with lexical, direction-swap, unsupported-edge,
abstention, source-family, and held-out-domain controls. Use deterministic tools
first and delegate inventory, mechanical checks, and test/log work to GPT-5.6
Luna; keep architecture, statistics, security, CCP, interpretation, release,
and merge decisions with GPT-5.6 Terra.

Do not load a model, access sealed targets, download new sources, or produce
model output until the protocol, fixtures, hashes, primary endpoint, and
resource envelope are frozen and an explicit operator approval is recorded.
After approval, perform exactly one guarded local SmolLM2 run, publish every
terminal outcome, the external dense locator/hash, exact-head CCP receipt, PR,
fresh-clone verification, limitations, and final report. Complete only when the
specification checklist is proven and no general TRIZ claim is promoted.

Current checkpoint: public no-model freeze commit `d8d4e98fdd4ad7145e427eb270f3a8ca23b844fb`, PR #75. Local R3 and repository gates pass. Material execution remains blocked by CCP admission `active=true` with an empty queue and two corrupt/incomplete operator-required journals; never bypass this gate or edit cache state manually.
