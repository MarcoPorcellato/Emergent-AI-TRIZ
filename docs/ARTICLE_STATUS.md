# Source article status

`SOURCE_ARTICLE.txt` is preserved from the text supplied by Marco Porcellato on 2026-08-13. It is the motivating research document for this repository.

## Important limitation

The supplied text contains citation tokens such as `cite...` that came from an external drafting or research interface. Those tokens are not stable bibliographic references and are intentionally preserved only as provenance markers.

Before treating the article as publication-ready:

1. map every imported token to a stable primary source;
2. verify factual claims against those sources;
3. replace tokens with a consistent citation format;
4. verify names, dates, model sizes, training-data claims, and project comparisons;
5. archive the reviewed bibliography and the exact article revision; and
6. record which claims are proposals, analogies, empirical findings, or open questions.

The repository README and protocol therefore make no empirical claim that the Latent TRIZ Hypothesis has already been demonstrated.

