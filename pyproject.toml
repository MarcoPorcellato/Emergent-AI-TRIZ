[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "latent-triz"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
latent-triz = "latent_triz.cli:main"
